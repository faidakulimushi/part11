name: Deployment pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [opened, synchronize]

permissions:
  contents: write

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Check style
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

      - name: Trigger deployment
        if: ${{ github.event_name == 'push' && !contains(join(github.event.commits.*.message, ' '), '#skip') }}
        run: curl ${{ secrets.RENDER_DEPLOY_HOOK }}

      - name: Notify Discord - Success
        if: ${{ success() && github.event_name == 'push' }}
        run: |
          curl -H "Content-Type: application/json" \
            -d "{\"content\":\"✅ Deployment successful! Repository: ${{ github.repository }} | Commit: ${{ github.sha }} | Branch: ${{ github.ref_name }}\"}" \
            "${{ secrets.DISCORD_WEBHOOK }}"

      - name: Notify Discord - Failure
        if: ${{ failure() }}
        run: |
          curl -H "Content-Type: application/json" \
            -d "{\"content\":\"❌ Build failed! Repository: ${{ github.repository }} | Commit: ${{ github.sha }} | Branch: ${{ github.ref_name }}\"}" \
            "${{ secrets.DISCORD_WEBHOOK }}"

  tag_release:
    needs: [simple_deployment_pipeline]
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && !contains(join(github.event.commits.*.message, ' '), '#skip') }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Bump version and push tag
        uses: anothrNick/github-tag-action@1.75.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BUMP: patch